// Fig. 5.2: ForCounter.cs
// Repetição controlada por contador com a estrutura for.

using System;

static class ForCounter
{
	static void Main(string[] args)
	{
		for (int counter = 1; counter <= 5; counter++)
			Console.WriteLine(counter);
	}
}
